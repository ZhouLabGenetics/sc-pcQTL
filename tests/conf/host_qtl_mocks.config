// Developer-only integration profile: core tasks remain containerized while
// the lightweight SAIGE-QTL shell mocks run directly on the test host.
process {
    withLabel: process_qtl {
        container = null
    }
}
